# Image decoder fixtures

`flat_red_64.heic` comes from the `heif-oxide` 0.1.0 test data. It is a
synthetic flat-color image distributed under MIT OR Apache-2.0 with that
crate and is used only to verify HEIC/HEIF decoding.
